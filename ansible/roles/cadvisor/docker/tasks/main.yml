---
- name: Create application directory
  file:
    path: "/opt/cadvisor"
    state: directory
    mode: '0755'

- name: Copy Docker Compose file
  template:
    src: compose.yml
    dest: "/opt/cadvisor/compose.yml"
    mode: '0644'

- name: Restart Docker Compose
  community.docker.docker_compose_v2:
    project_src: "/opt/cadvisor"
    files:
      - compose.yml
    state: present