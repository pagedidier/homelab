services:
  node_exporter:
    image: quay.io/prometheus/node-exporter:v1.9.1
    restart: unless-stopped
    container_name: node_exporter
    command:
      - '--path.rootfs=/host'
    pid: host
    volumes:
      - '/:/host:ro,rslave'
    ports:
      - "9100:9100"