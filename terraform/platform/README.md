# Plateform

This folder contains the configuration for the _plateform layer_.
It use terraform to configure the namespace and rbac in k8s, the gitlab-ci variables, the vault path and policies.

In the future, it will configure the databases, the proxies and the monitoring