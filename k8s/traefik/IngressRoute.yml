apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-dashboard
  namespace: kube-system
spec:
  entryPoints:
    - web-private
  routes:
    - match: Host(`traefik.k3s.prod.twop.ch`)
      kind: Rule
      services:
        - name: api@internal
          kind: TraefikService