apiVersion: v1
kind: Service
metadata:
  namespace: plex
  name: plex
  labels:
    app: plex
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 32400
      protocol: TCP
  selector:
    app: plex