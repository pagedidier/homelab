apiVersion: v1
kind: Service
metadata:
  name:  {{ .Chart.Name }}-api-service-{{ .Values.environment }}
spec:
  selector:
    app:  {{ .Chart.Name }}-api-{{ .Values.environment }}
  type:  ClusterIP
  ports:
    - name:  http
      port:  80
      targetPort: api-port