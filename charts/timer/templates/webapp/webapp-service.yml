apiVersion: v1
kind: Service
metadata:
  name:  {{ .Chart.Name }}-webapp-service
spec:
  selector:
    app:  {{ .Chart.Name }}-webapp
  type:  ClusterIP
  ports:
    - name:  http
      port:  80
      targetPort: webapp-port