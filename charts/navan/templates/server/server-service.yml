apiVersion: v1
kind: Service
metadata:
  name:  {{ .Chart.Name }}-server-service-{{ .Values.environment }}
spec:
  selector:
    app:  {{ .Chart.Name }}-server-{{ .Values.environment }}
  type:  ClusterIP
  ports:
    - name:  service-server-port
      port:  80
      targetPort: server-port