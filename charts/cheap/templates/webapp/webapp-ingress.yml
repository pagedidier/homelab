apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Chart.Name }}-webapp-ingress-{{ .Values.environment }}
  annotations:
    kubernetes.io/ingress.ingressClassName: "traefik"
    traefik.ingress.kubernetes.io/router.entrypoints: {{ .Values.entrypoint }}
spec:
  rules:
    - host: "{{ if .Values.previewPrefix }}{{ .Values.previewPrefix }}.{{ end }}{{ .Values.domain }}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Chart.Name }}-webapp-service-{{ .Values.environment }}
                port:
                  name: svc-webapp-port