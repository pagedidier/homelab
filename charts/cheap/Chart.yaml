apiVersion: v2
name: cheap
description: A Helm chart for cheap app
type: application
version: 0.0.1

dependencies:
  - name: mysql
    version: "14.0.3"
    repository: "https://charts.bitnami.com/bitnami"
    condition: mysql.enabled